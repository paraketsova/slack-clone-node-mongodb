<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Info from MongoDB</title>
</head>
<body>
  
  <div id="root">
    
    <div id="dashboard">
      <h2>Slack-clon</h2>
      <div id = userInfo>
        <h3>User Info:</h3>
      </div>
      
      <div id="channels">
        <h3>Channels:</h3>
      <ul id="channelsContainer">
        <% channels.forEach( channel => { %>
          <li id="channelUnit">
            
          </li>
        <% }) %>
      </ul>
      </div>  
    </div>
  
    <div id="messages"></div>
      <strong>Messages:</strong>
      <ul  id="messagesContainer">
        <% messages.forEach( message => { %>
          <li>
            <p>From: <%= message.user.username %></p>
            <p>Channel: <%= message.channel.name %></p>
            <p><%= message.timestamp %></p>
            <p><%= message.text %></p>
          </li>
        <% }) %>
      </ul>
    </div>
  </div>
</body>
</html>